import {ThemedElement} from "./ThemedElement.js";
import {ThemedElementProps} from "../types";

export const IdleCell = (props: Partial<ThemedElementProps>) => <ThemedElement Component={"div"} {...props} box/>;
